## Usage

```javascript
const GridlyClient = require('gridly-client-js');

const gridlyClient = new GridlyClient({YOUR_API_KEY});
const records = await gridlyClient.records({VIEW_ID}).get();

console.log(records);
```
